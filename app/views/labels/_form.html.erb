<%= form_with(model: label, local: true) do |form| %>
  <% if lable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lable.errors.count, "error") %> prohibited this label from being saved:</h2>

      <ul>
      <% label.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :label_name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :label_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
